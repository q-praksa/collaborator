export type userCredentialsType = {
    email: string;
    password: string;
};

export type employeePayloadType = {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    jobTitle: string;
};
