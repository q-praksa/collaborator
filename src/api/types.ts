export type userCredentialsType = {
    email: string;
    password: string;
};

export type clientPayloadType = {
    id: string;
    companyName: string;
    CEO: string;
    img: string;
    region: string;
};
