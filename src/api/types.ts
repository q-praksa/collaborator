export type userCredentialsType = {
    email: string;
    password: string;
};

export type employeePayloadType = {
    fullname: string;
    email: string;
    password: string;
    job: string;
};
